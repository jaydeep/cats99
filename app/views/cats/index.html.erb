<h1>All of 99Cats' cats</h1>
<ul>
	<% @cats.each do |cat| %>
	<li>
		<%= cat.name %>

	  <%= link_to "Cat page", cat_url(cat.id) %>
	</li>
	<% end %>
</ul>

<%= button_to "New cat", new_cat_url, :method => :get %>

<%= button_to "Make rental request", new_cat_rental_request_url, :method => :get %>